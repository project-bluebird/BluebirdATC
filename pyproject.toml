[tool.uv.workspace]
members = [
  "bluebird-dt",
  "bluebird-api",
  "bluebird-gymnasium"
]

[tool.pytest.ini_options]
testpaths = [
  "bluebird-dt/tests",
  "bluebird-api/tests",
  "bluebird-gymnasium/tests"
]

addopts = ["--import-mode=importlib"]
